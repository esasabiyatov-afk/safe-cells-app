# Приёмочные и автоматические проверки

## 1. Общие правила тестов

- Используется `pytest` и временные локальные каталоги.
- Реальные сетевые пути и персональные данные запрещены.
- Дата передаётся в функцию явно; прямой `datetime.now()` в тестируемой бизнес-логике не используется.
- Тесты проверяют состояние обеих баз после успеха и после ошибки.
- Производственный `busy_timeout` равен 15000 мс; тест может подставить меньший таймаут, сохраняя тот же путь обработки.
- Успешная команда и точное число тестов записываются в `PROJECT_STATUS.md` после каждого этапа.

## 2. Матрица обязательных автоматических сценариев

| Область | Минимальные проверки |
|---|---|
| CSV/seed | 126 строк; номера уникальны и равны 1–126; нет пустых полей; только 50/75/100/125/175/300; повторная инициализация не добавляет строки. |
| Тарифы | Все 6 высот × границы 1, 30, 31, 90, 91, 180, 181; один тариф применяется ко всему сроку; неизвестная высота/пробел диапазона дают понятную ошибку. |
| Даты аренды | Один день; обе даты включены; `end < start`; ноль/отрицательные дни; вычисление конца по дням; backend отклоняет подменённое число дней. |
| Статусы | При `as_of=2026-07-01`, порог 7: конец 2026-06-30 — overdue; 2026-07-01 и 2026-07-08 — expiring; 2026-07-09 — normal; свободная — free. |
| Штраф продления | Старый конец 2026-06-27: продление 27/28 июня — 0 дней; 29 июня — 1; ставка всегда диапазона 1–30. |
| Штраф закрытия | Конец 2026-06-27: закрытие 27 июня — 0; 28 июня — 1; 29 июня — 2. |
| Read-only | URI содержит `mode=ro`; запись и DDL отклоняются; соединение закрывается после результата и исключения. |
| Недоступная сеть | Нет общей папки/БД/прав; показано утверждённое сообщение; локальная БД не появляется. |
| Занятие | Успех; обязательные поля; занятая ячейка; два конкурентных запроса; DB `UNIQUE`; повторный `operation_id`; lock; rollback после искусственного сбоя. |
| Переходы | Нельзя занять занятую, продлить/закрыть свободную, повторно закрыть архивный договор или удалить историю. |
| Продление | Актив и renewal меняются вместе; своевременно/на следующий день/просрочено; повтор и rollback; старая история сохраняется. |
| Закрытие | Архивная вставка и удаление актива одним `COMMIT`; три вида закрытия; сбой до/после архивной вставки; повтор; ячейка освобождается только после успеха. |
| Конкуренция | Реальная вторая SQLite-сессия держит write lock; приложение ждёт настроенный срок, показывает понятную ошибку и не оставляет частичных данных. |
| Приватность | Общий API/HTML не содержат ФИО, ID и счёт; технические логи не содержат тестовые секретные маркеры; XSS-строки экранированы. |
| Документы | Неизвестный/отсутствующий плейсхолдер; обязательное поле; таблицы/колонтитулы; безопасное имя; path traversal; сбой публикации после `COMMIT`; реестра файлов нет. |
| Администратор | Стойкий хеш с солью; пароль не логируется; общий пароль и предупреждение без пароля; пересекающиеся тарифы и неизвестный config запрещены; аудит изменений. |
| Бэкапы | Две копии и манифест; SQLite `quick_check`; контрольные суммы; временный неполный комплект не считается; ротация до 30; повреждение; отказ между копиями. |
| Восстановление | Не запускается автоматически; требует административный режим; сохраняет повреждённые файлы; восстанавливает обе базы из одного валидного комплекта. |
| Runtime | Только loopback; свободный порт; токен экземпляра; закрытие одного процесса не влияет на другой; обновление страницы не завершает процесс. |

## 3. Проверки этапа 0

Выполнено 12 июля 2026 года:

- CSV прочитан машинно: 126 строк, 126 уникальных номеров, диапазон 1–126, пропусков/дубликатов/пустых полей/недопустимых высот нет.
- Распределение высот: 50 мм — 45, 75 мм — 13, 100 мм — 56, 125 мм — 4, 175 мм — 5, 300 мм — 3.
- Корневые копии ТЗ, архитектуры, CSV и прототипа совпадают с каноническими файлами по SHA-256.
- Код и pytest-тесты ещё не существуют; утверждение об их прохождении не делается.

## 4. Ручная приёмка по этапам

1. После этапа 2 — внешний вид, понятность цветов, высот, фильтров и сетевой ошибки.
2. После этапа 9 — визуальная сверка каждого реального DOCX с утверждённым банковским шаблоном.
3. После этапа 12 — полный путь: запуск, поиск, расчёт, занятие, продление, закрытие, документ, бэкап и второй экземпляр.
4. До допуска в банк — тест на фактическом сетевом томе только с вымышленными данными и 1–3 компьютерами.

## Результат этапа 10

- полный набор: `207 passed` на Python 3.14.6;
- пароль хранится как `scrypt`-хеш с уникальной солью и не присутствует в административном аудите;
- неверные попытки не блокируют общий пароль; доступ живёт в памяти до закрытия окна, смена пароля отменяет прежний токен;
- проверено переключение между общим паролем и предупреждением без пароля;
- без токена чтение и запись настроек запрещены;
- проверены 24 тарифа, полный набор высот, пропуск/пересечение диапазонов, отрицательная сумма и неизвестный config-ключ;
- повтор операции создаёт одну запись аудита, write-lock и искусственный сбой не оставляют частичного изменения;
- проверены добавление, изменение и отключение сотрудников, запрет дублей и последнего активного сотрудника, а также независимый выбор в двух локальных процессах;
- DOCX проверяется на имя, размер, распакованный размер и известные поля; файл без полей разрешён, выход за `templates` отклоняется;
- в браузере проверены пять вкладок, карточки шаблонов, оба режима входа, обязательный выбор сотрудника, компактная шестерёнка, отсутствие ручной кнопки обновления и ошибок консоли.
- в браузере проверен быстрый ввод даты продления до первого ответа: дни, начало периода и сумма рассчитываются, блок штрафа имеет две колонки без пустой заливки.

## Результат этапа 9

- полный набор после подключения автоматических комплектов: `188 passed`;
- тестовый DOCX заполнен в абзаце, таблице и колонтитуле, в том числе при разрыве плейсхолдера форматированием;
- отсутствующие и неизвестные поля, повреждённые настройки и выход за каталог отклоняются до публикации;
- файл атомарно появляется в «Загрузках», временный файл удаляется, реестр генераций не создаётся;
- защищённый маршрут не раскрывает данные без токена и отклоняет устаревший договор;
- в браузере отсутствие активных шаблонов показано понятным сообщением без ошибок JavaScript.
- схема 2→3 обновляет обе базы только явной командой, после бэкапа; искусственный сбой откатывает обе базы;
- общий справочник хранит активных сотрудников, а выбор для каждого локального процесса не зависит от Windows-учётки;
- в реальном договоре заполнены 18 уникальных кодов в 27 местах; остаточных кодов нет, структура DOCX сохранена.
- все пять файлов комплекта заполнены на вымышленных данных без остатков кодов и изменения структуры;
- автоматический тест сравнивает точные внутренние значения верхнего, нижнего, левого и правого полей до и после заполнения.
- после открытия формируются три документа, после продления и закрытия — по одному;
- сбой DOCX после сохранения возвращается отдельным предупреждением, не удаляет договор и не оставляет частично опубликованный комплект;
- явная инициализация регистрирует только присутствующие файлы пяти утверждённых типов.

## 5. Результат этапа 1

Проверено 12 июля 2026 года на Python 3.14.6:

- полный набор: `30 passed`;
- импортированы 126 уникальных ячеек и 24 тарифа;
- повторная инициализация не создаёт дубликаты и не перезаписывает изменённый тариф;
- обе базы проходят SQLite `quick_check`;
- `contracts.cell_number` не допускает второй активный договор;
- read-only соединение отклоняет запись и закрывается после операции;
- write-соединение применяет DELETE journal, FULL synchronous, NORMAL locking, foreign keys и таймаут 15000 мс;
- недоступный путь, неполная пара баз, неверный seed и несовместимая версия схемы обрабатываются без создания локальной подмены.

## 6. Результат этапа 2

Проверено 12 июля 2026 года:

- полный набор после уточнений: `52 passed` в строгом режиме предупреждений;
- статусы free/normal/expiring/overdue проверены на граничных датах с фиксированной датой 01.07.2026;
- API возвращает 126 ячеек и не возвращает ФИО, ID-карту или номер счёта;
- поиск по приватным полям использует POST и возвращает только номера совпавших ячеек;
- HTML экранирует имя Windows-пользователя и не содержит внешних ресурсов;
- CSP разрешает загрузку только локальных ресурсов;
- недоступная база даёт утверждённое сообщение и не создаёт локальную замену;
- браузер: поиск по вымышленному ФИО — 1 результат, фильтр просрочки — 5, высота 300 мм — 3;
- браузер: карточка ячейки показывает только размер, статус, дату и дни;
- браузер: ручное обновление и интервал 15 секунд работают, ошибок консоли нет;
- браузер: при остановке тестового сервера старая сетка скрывается, счётчики заменяются на «—»;
- ширина 420 и 1280 пикселей проверена без горизонтального переполнения.
- денежные настройки seed: сом (`KGS`), масштаб 0, залог 1500; повторная инициализация не перезаписывает административное значение;
- усиленные статусы повторно проверены в браузере: разные насыщенные фоны, тёмные рамки толщиной 2 CSS-пикселя и цветные внутренние полосы.

## 7. Результат этапа 3

Проверено 12 июля 2026 года:

- полный набор: `85 passed` в строгом режиме предупреждений;
- включительный срок и вычисление даты окончания проверены на границах 1, 30, 31, 90, 91, 180 и 181 день;
- тарифные границы проверены для всех шести высот, один тариф применяется ко всему сроку;
- backend отклоняет неверные даты, ноль и отрицательные дни, подменённое количество дней и занятую ячейку;
- расчёт читает тариф и залог через короткое `mode=ro`-соединение и не изменяет договоры;
- недоступная база даёт утверждённую сетевую ошибку без создания локальной замены;
- браузер: для ячейки высотой 50 мм срок 30 дней даёт 450 сом аренды;
- браузер: переход на 31 день меняет дату окончания и тариф и даёт 310 сом аренды;
- залог 1500 сом показан отдельным блоком и не складывается с арендой;
- в форме нет выбора валюты, дробных сумм и объединённой строки «Итого к оплате», кнопка записи отключена.

## 8. Результат этапа 4

Проверено 12 июля 2026 года:

- полный набор: `107 passed` в строгом режиме предупреждений;
- обязательные поля договора и формат дат проверяются на frontend для удобства и повторно на backend;
- тариф, стоимость, залог, срок и свободность ячейки перечитываются внутри `BEGIN IMMEDIATE`;
- договор в рабочей базе и обезличенный аудит в архивной базе фиксируются одним `COMMIT`;
- повторный `operation_id` возвращает существующий договор без второй записи;
- два параллельных запроса оставляют ровно один активный договор;
- реальный write-lock даёт понятную ошибку и не оставляет частичных данных;
- искусственный сбой после вставки договора откатывает и договор, и аудит;
- разрыв после фактического `COMMIT` даёт предупреждение о неопределённом результате; безопасный повтор определяет сохранённую операцию;
- ответ API и технические логи не содержат ФИО, ID-карту и номер счёта;
- после успеха SQLite Backup API создаёт две копии, обе проходят `quick_check`;
- браузер: пустая форма блокируется, вымышленный договор сохраняется, счётчик свободных уменьшается, ячейка меняет статус, сообщение об успехе показывается без ошибок консоли.

## 9. Результат этапа 5

Проверено 12 июля 2026 года:

- полный набор: `118 passed` в строгом режиме предупреждений;
- общий список содержит только утверждённое сокращение «Фамилия И. О.» и не содержит полного ФИО, ID-карту или счёт;
- private endpoint принимает только POST и требует временный токен текущего экземпляра;
- запрос дополнительно проверяет номер ячейки и точный идентификатор ожидаемого активного договора;
- свободная ячейка и устаревшая карточка не раскрывают данные;
- рабочая и архивная базы читаются короткими `mode=ro`-соединениями, которые закрываются после результата;
- история продлений возвращается только private endpoint и остаётся свёрнутой по умолчанию;
- XSS-маркер отсутствует в исходном HTML и общем API и выводится frontend только через `textContent`;
- браузер: до нажатия «Показать данные» персональных значений в DOM нет;
- браузер: после нажатия данные раскрываются, история остаётся отдельным закрытым разделом;
- браузер: «Скрыть данные», закрытие карточки и потеря общей базы очищают персональные значения; ошибок консоли нет.

## 10. Результат этапа 6

Проверено 12 июля 2026 года:

- полный набор: `138 passed` в строгом режиме предупреждений;
- утверждённое начало периода проверено для оформления заранее, в дату окончания, на следующий день и после разрыва;
- формула штрафа проверена на нулевой и положительной границе, ставка всегда берётся из диапазона 1–30 дней;
- тариф продления проверен на границах 1/30/31/90/91/180/181 день;
- рабочая дата окончания, `archive.renewals` и `archive.log` фиксируются одним `COMMIT`;
- первоначальные срок, ставка и сумма договора после продления не меняются;
- изменение тарифа не меняет ставку/сумму открытого договора и прошлой истории, а следующее продление использует новую ставку и сохраняет её собственным снимком;
- повторный `operation_id` не создаёт вторую историю; два конкурентных запроса дают один успех и один конфликт;
- write-lock, недоступный путь, искусственный частичный сбой и разрыв после `COMMIT` обработаны без частичной записи;
- ответ API и аудит не содержат персональные данные;
- браузер: 31 день для ячейки высотой 50 мм рассчитан по 10 сом/день, сумма 310 сом, после сохранения карточка показывает новую дату и история содержит одну запись; ошибок консоли нет.

## 11. Исправление проверки текущей даты

Проверено 12 июля 2026 года:

- полный набор: `142 passed` в строгом режиме предупреждений;
- калькулятор и подтверждение договора отклоняют начало 13.07.2026 при серверной дате 12.07.2026;
- уже сохранённый активный договор с будущим началом определяется как ошибка целостности, а не как обычный статус;
- в браузере ячейка 9 показывает начало 01.07.2026, окончание 15.07.2026 и статус «Истекает»;
- браузер блокирует продолжение при будущем начале и показывает понятное сообщение без ошибок консоли.

## 12. Результат этапа 7

Проверено 12 июля 2026 года:

- текущий полный набор: `164 passed` в строгом режиме предупреждений;
- досрочное, своевременное и просроченное закрытие проверены на границах штрафных дней;
- оплаченная аренда всегда имеет возврат `0`, произвольного поля удержаний нет;
- «Потеря ключа» фиксируется причиной и даёт возврат залога `0`; обычное закрытие возвращает полный залог;
- архивная запись, обезличенный аудит и удаление актива выполняются одним `COMMIT` через `ATTACH DATABASE`;
- история продлений сохраняется после закрытия;
- повтор, два конкурентных запроса, write-lock, два частичных сбоя, недоступная сеть и разрыв после `COMMIT` не создают промежуточного состояния;
- браузер: ячейка 9 закрыта досрочно с причиной «Потеря ключа», стала свободной, архив содержит причину и нулевой возврат залога; обе базы проходят `quick_check`, ошибок консоли нет.
