# Состояние проекта

Дата обновления: 16 июля 2026 года.

## Текущий статус

Этапы 0–11.5 технически завершены. После каждой подтверждённой записи создаётся атомарно публикуемый проверенный комплект двух баз с манифестом и SHA-256. Административный раздел показывает целостность и резервные комплекты; восстановление выполняется только вручную после обнаружения повреждения и проверки, что другие экземпляры закрыты. Обычному выбранному сотруднику доступен общий журнал занятия, продления и закрытия с ФИО клиента и Excel-выпиской. Этап 12 не начат.

## Готово

- Полностью прочитаны мастер-промпт, `AGENTS.md`, ТЗ, утверждённая архитектура, CSV и HTML-прототип.
- Проверено, что профильные и корневые копии четырёх исходных файлов совпадают.
- Проверен CSV: 126 уникальных ячеек без пропусков, дублей и недопустимых высот.
- Создана архитектура чтения, записи, конкуренции, двух баз, документов, бэкапов и восстановления.
- Описаны модель данных, решения, открытые бизнес-вопросы, этапы, тесты и трассировка.
- Создан локальный Git-репозиторий и исходный коммит `356e475 Initial project requirements`.
- Добавлен `.gitignore` для БД, бэкапов, документов, секретов, локальных конфигов, сборки и переносимого инструмента Git.
- Утверждено: сотрудник нажимает веб-ярлык на рабочем столе; локальный `.exe` запускается скрыто и открывает браузер.
- Создан модульный Flask-каркас и локальная JSON-конфигурация без секретов.
- Реализована отдельная подтверждаемая команда инициализации двух баз; обычный запуск базы не создаёт.
- Созданы полные таблицы рабочей и архивной БД, 126 ячеек, 24 тарифа и начальный config.
- Реализованы короткие `mode=ro` соединения и подготовка write-соединения с обязательными PRAGMA.
- Добавлены тесты схемы, seed, ограничений, недоступного пути, повторной инициализации и Flask health route.
- Реализован read-only API 126 ячеек без массовой передачи персональных данных.
- Добавлены вычисляемые статусы, счётчики, точные высоты, поиск и фильтры.
- Поиск ФИО/счёта выполняется POST-запросом и возвращает только номера ячеек.
- Главный экран использует только локальные HTML/CSS/JS и обновляется каждые 15 секунд.
- При потере сети ранее загруженная сетка скрывается, а запись не выполняется.
- Владелец требований одобрил главный экран и усиленные цвета статусов.
- Добавлен read-only калькулятор свободной ячейки с синхронизацией дат и количества дней.
- Тариф выбирается по полной длительности аренды; суммы рассчитываются только в целых сомах.
- Залог читается из общей настройки и по умолчанию равен 1500 сом; выбора валюты в форме нет.
- Перед каждым расчётом backend повторно проверяет, что ячейка свободна; калькулятор не изменяет базу.
- Добавлена форма обязательных данных клиента, ID-карты, счёта и договора.
- Даты, тариф, сумма аренды и залог повторно рассчитываются внутри `BEGIN IMMEDIATE`; значения браузера не считаются доверенными.
- Активный договор и запись `archive.log` создаются одной транзакцией через `ATTACH DATABASE`.
- `operation_id` защищает от двойного нажатия и безопасно определяет уже сохранённую операцию.
- Конкурентное занятие, длительная блокировка, частичный сбой и неопределённый результат записи обрабатываются без автоматического повтора.
- После успешной записи создаётся проверенный комплект рабочей и архивной баз через SQLite Backup API.
- Общий список содержит для занятой ячейки сокращённое имя «Фамилия И. О.»; остальные персональные поля не передаются.
- Полное ФИО запрашивается отдельным защищённым POST-запросом только при открытии конкретной занятой карточки; ID-карта и счёт запрашиваются кнопкой «Показать данные».
- Private-запрос проверяет точный активный договор, поэтому устаревшая карточка не может раскрыть данные нового клиента той же ячейки.
- История продлений остаётся свёрнутой даже после раскрытия данных клиента.
- Скрытие, закрытие карточки и сетевая ошибка очищают персональные значения из DOM.
- Продление рассчитывает начало нового периода только по утверждённому правилу D-022; сотрудник вводит новую дату окончания или количество дней.
- Стоимость продления выбирается по полной длительности, а штраф использует выбранный администратором режим: тариф 1–30 дней либо отдельную ручную ставку точной высоты.
- Штрафные дни продления считаются отдельно: день оформления нового периода не включается в штраф.
- Подтверждение одной транзакцией обновляет текущую дату окончания, добавляет неизменяемую запись `archive.renewals` и обезличенный аудит.
- Изменение справочника тарифов не пересчитывает финансовые снимки открытых договоров и прошлых продлений; новое продление использует тариф на момент подтверждения и сохраняет его отдельно.
- Повтор операции, конкурентное изменение даты, write-lock, частичный сбой и неопределённый результат обрабатываются без дублирования или частичных данных.
- Первоначальные срок, тариф и сумма договора сохраняются для расчётов и архива, но не показываются в оперативной карточке.
- Дата начала активной аренды сверяется с текущей серверной датой: будущее начало блокируется в калькуляторе и повторно перед записью; невозможные сохранённые данные не получают ложный статус.
- Закрытие различает досрочный, своевременный и просроченный сценарии; штраф закрытия считается по отдельной формуле ТЗ.
- Оплаченная аренда не пересчитывается и не возвращается; отдельной функции удержаний нет.
- При причине «Потеря ключа» архив фиксирует эту причину и нулевой возврат залога; иначе показывается полный залог к возврату.
- Полный договор переносится в `contracts_archive`, аудит создаётся и актив удаляется одной транзакцией; история продлений сохраняется.
- DOCX-плейсхолдеры проверяются и заполняются в абзацах, таблицах, верхних и нижних колонтитулах, включая разрыв по форматированным фрагментам.
- Шаблоны разрешены только по активной записи БД и безопасному имени внутри общей папки `templates`; выход за каталог блокируется.
- Готовый файл сначала создаётся под временным именем, затем атомарно публикуется в локальные «Загрузки»; внутренний реестр документов не ведётся.
- В карточке активного договора подключено повторное формирование; при отсутствии шаблонов показано честное сообщение без создания файла.
- Схема 3 заменяет ошибочную дату окончания ID-карты датой выдачи; явная миграция 2→3 создаёт бэкап и меняет обе базы одной транзакцией.
- Сотрудники ведутся в общем справочнике; при каждом запуске обязательно выбирается активный сотрудник без связи с Windows-учёткой, а выбор хранится только в памяти локального процесса.
- Настройки открываются компактной шестерёнкой; ручная кнопка «Обновить» убрана, автоматическое обновление каждые 15 секунд сохранено.
- После открытия, продления и закрытия отдельное окно перечисляет сформированные DOCX и папку «Загрузки» либо явно сообщает о сбое документов при уже сохранённой операции.
- В форме продления взаимный пересчёт даты и дней не зависит от предыдущего ответа сервера; блок штрафа отображается только после расчёта и без пустой колонки.
- Реальный `Договор индивидуального сейфа ф.л.docx` поддерживает 18 квадратных кодов и 27 мест вставки без изменения структуры документа.
- `[Договор.НачалоД]` и `[Договор.НачалоДК]` всегда используют первоначальную дату договора; шесть кодов `[Продление.*]` берут даты, сумму и срок конкретного продления.
- Все пять файлов утверждённого комплекта заполняются без остатков кодов; структура DOCX сохраняется.
- Явная инициализация регистрирует только реально скопированные в общую папку `templates` файлы утверждённого комплекта.
- DOCX-комплект формируется после `COMMIT`; ошибка шаблона или сохранения не отменяет бизнес-операцию и показывается отдельным предупреждением.
- Многофайловый комплект сначала полностью проверяется во временной папке, поэтому ошибка одного шаблона не публикует остальные файлы частично.
- Поля основного договора сохраняются точно: 1,5/1,1/2,5/1,5 см для верхнего/нижнего/левого/правого поля.
- Первый административный пароль создаётся только явным действием; в общей базе хранится `scrypt`-хеш с уникальной солью, исходный пароль не попадает в аудит.
- В настройках можно выбрать общий пароль отдела или вход без пароля после обязательного подтверждения ответственности руководителя; блокировки попыток и автоматического выхода по времени нет.
- Технический токен доступа существует только в памяти локального процесса до закрытия окна; при парольном режиме в БД по-прежнему хранится только `scrypt`-хеш с солью.
- Из настроек разрешено изменять порог «истекает», сумму залога, полный набор обычных тарифов, режим штрафа и полный набор ручных штрафных ставок; валюта, масштаб и диапазоны срока не меняются через интерфейс.
- Backend требует непрерывный полный набор тарифов для всех фактических высот, отклоняет отрицательные суммы, пропуски, пересечения и неизвестные config-ключи.
- Административные записи используют `BEGIN IMMEDIATE`, повторно читают данные внутри транзакции, создают аудит через `ATTACH DATABASE` и бэкап после успеха.
- DOCX можно добавить или заменить только внутри общей папки `templates`; файл ограничен по размеру, открывается и проверяется на известные поля до регистрации. Шаблон без полей разрешён как готовый документ без заполнения.
- Вкладка шаблонов показывает имя файла, назначение, наличие файла и найденные поля; исходную копию можно получить отдельной кнопкой.
- Добавление новых ячеек намеренно не реализовано как функция будущей версии.
- Резервный комплект создаётся единым существующим механизмом через SQLite Backup API, пока общая межпроцессная блокировка не допускает конкурентную запись между снимками двух баз.
- Обе копии сначала находятся в скрытом временном каталоге, проходят `quick_check`, получают размеры и SHA-256; манифест проверяется до атомарной публикации всего каталога.
- Ротация хранит последние 30 завершённых проверенных комплектов и не считает временные или неполные каталоги опубликованными.
- Перед каждой записью проверяется постоянный маркер повреждения и целостность обеих активных баз; после обнаружения повреждения все обычные записи блокируются без автоматического восстановления.
- Каждый локальный экземпляр удерживает обезличенный файловый маркер. Восстановление запрещено, если нельзя подтвердить регистрацию текущего экземпляра или найден другой активный экземпляр.
- Контролируемое восстановление принимает только полный комплект с верным манифестом, SHA-256 и `quick_check`, сохраняет обе исходные базы в `damaged-originals`, затем восстанавливает пару и создаёт обезличенный аудит.
- Автоматического повтора неопределённой записи и автоматического восстановления нет. Обычное `shutil.copy` для активных баз и замена активной базы после каждой записи не используются.
- В настройки добавлена шестая вкладка «Резервные копии» с состоянием обеих баз, числом других экземпляров, списком комплектов и явно подтверждаемой кнопкой восстановления.
- На главном экране добавлена кнопка «Журнал ячеек» с занятиями, продлениями и закрытиями по всем ячейкам; события исправления полностью исключены.
- Журнал использует существующий `archive.log`, короткие `mode=ro`-чтения обеих баз, фильтры по номеру/действию/датам и страницы по 50 записей; схема и запись аудита не менялись.
- API журнала показывает ФИО из активного договора или архивного снимка, но исключает административные события, ID-карту, счёт, договорные идентификаторы и старые/новые значения исправлений.
- Плашки и Excel используют названия «Открытие», «Продление» и «Закрытие»; при штрафных днях добавляется «Просрочка» и доступен отдельный фильтр.
- Открытие и продление показывают период и срок в днях; закрытие показывает причину без повтора даты и слова «Причина».
- Кнопка «Скачать отчёт Excel» формирует настоящий `.xlsx` из текущего отбора; файл не содержит исправлений и секретных реквизитов, а потенциальные Excel-формулы нейтрализуются.

## Проверено автоматически

- CSV: 126 строк; номера 1–126; уникальность 126; пустых полей 0; недопустимых высот 0.
- Высоты: 50/75/100/125/175/300 мм, распределение 45/13/56/4/5/3.
- SHA-256: дубликаты исходных ТЗ, архитектуры, CSV и HTML совпадают.
- Git: исходный коммит создан локально.
- `pytest`: 243 теста успешно пройдены на Python 3.14.6.
- Денежные seed-настройки проверены: `KGS`, масштаб `0`, залог `1500`.
- Контраст статусов усилен и повторно проверен в браузере на рабочей сетке.
- Калькулятор проверен на границе 30/31 день: сумма аренды 450 и 310 сом соответственно; залог 1500 сом показан отдельно и не складывается с арендой.
- Занятие проверено при двух одновременных запросах, повторном запросе, write-lock, откате между вставками и разрыве после `COMMIT`.
- Браузер: обязательные поля блокируют пустую форму; тестовый договор меняет ячейку 41 со свободной на занятую, обновляет счётчики и показывает сообщение об успехе.
- Браузер и архив: ошибок консоли нет; аудит не содержит ФИО, ID-карту и номер счёта; создана пара валидных бэкапов.
- Карточка проверена в браузере в трёх состояниях: данные скрыты, раскрыты явной кнопкой и полностью очищены после скрытия/закрытия.
- Private API без токена или с неверным/устаревшим договором отклоняет запрос; XSS-маркеры передаются только как текст.
- SQLite `quick_check`: обе временные тестовые базы проходят проверку.
- Продление проверено на границах 1/30/31/90/91/180/181 день, при оформлении заранее, в дату окончания, на следующий день и после просрочки.
- Тарифный снимок проверен последовательностью: договор по старой ставке → изменение тарифа → продление по новой ставке → повторное изменение тарифа без изменения договора и истории.
- Браузер: 31 день для высоты 50 мм рассчитан по 10 сом, итог 310 сом; после подтверждения карточка и история обновились, ошибок консоли нет.
- Браузер: демонстрационная ячейка 9 начинается 01.07.2026 и корректно истекает 15.07.2026; ввод начала 13.07.2026 при текущей дате 12.07.2026 заблокирован.
- Браузер: прямоугольная плитка показывает фамилию с инициалами и высоту без «мм»; карточка показывает размер `100×220×330`; нулевые штрафные блоки продления и закрытия скрыты.
- Браузер 1500×900: первый экран вмещает 78 плиток; счётчик занятых равен сумме трёх занятых статусов; карточка показывает полное ФИО без дубликата и общий срок без скобок; ошибок консоли нет.
- После переноса сводки в шапку экран 1500×900 вмещает 104 плитки; легенда использует четыре простых контрастных цвета, ФИО на плитках уменьшено.
- Браузер: досрочное закрытие ячейки 9 с причиной «Потеря ключа» показало 0 сом возврата залога, перенесло договор в архив и сделало ячейку свободной; ошибок консоли нет.
- Браузер: кнопка повторного формирования доступна; без настроенных шаблонов показано «Активные DOCX-шаблоны пока не настроены», ошибок консоли нет.
- DOCX-тест: вымышленные значения заполнены в разорванном плейсхолдере, таблице и колонтитуле; неизвестные/отсутствующие поля и path traversal отклонены.
- Миграция 2→3 проверена с сохранением активного договора, бэкапом старой схемы и откатом обеих баз при искусственном частичном сбое.
- Реальный договор: заменены все 27 кодов; сохранены 232 абзаца, 4 таблицы, 471 форматированный фрагмент и изображение; остаточных кодов нет.
- Все пять реальных файлов комплекта: все известные коды заменены на вымышленных данных, количество абзацев, таблиц, фрагментов, изображений и поля страницы сохранены.
- Браузер: проверены пустой общий справочник, добавление тестового сотрудника, обязательный выбор при запуске, отображение выбранного имени в шапке, пять вкладок, компактная шестерёнка и отсутствие ручной кнопки обновления; ошибок консоли нет.
- Администратор: проверены случайная соль хеша, отсутствие блокировки неверных попыток, оба режима входа, завершение доступа при закрытии, смена пароля, запрет неизвестных config-ключей, отрицательных сумм, пропусков и пересечений тарифов.
- Администратор: повтор операции не дублирует аудит; write-lock и искусственный сбой не оставляют частичных настроек.
- DOCX-настройки: проверены добавление, отключение, статический DOCX без полей, получение копии, неизвестное поле, выход за каталог и отсутствие открытого пароля в журнале.
- Браузер: проверены четыре верхние вкладки, карточки DOCX, отсутствие горизонтального переполнения, переключение на предупреждение без пароля и возврат к общему паролю; новых ошибок консоли нет.
- Браузер 1280 px: шапка переносит сводку на отдельную строку без пересечений; кнопка начала работы отделена от списка, а компактный блок залога расположен справа.
- Администратор: на вкладке тарифов выбирается автоматическая ставка 1–30 дней либо независимый полный набор из шести ручных ставок; переключение не удаляет сохранённые ручные значения.
- Продление и закрытие повторно читают режим штрафа внутри write-транзакции и сохраняют фактическую ставку неизменяемым архивным снимком.
- Защищённая карточка показывает сотрудника открытия договора, а история — сотрудника каждого продления; общий список ячеек эти данные не получает.
- Повторное формирование DOCX возвращает и показывает имя реально опубликованного файла; в браузере подтверждено появление документа в локальных «Загрузках».
- Калькулятор открывается с пустым полем дней; поддержана вставка даты `ДДММГГГГ`, а рабочие диалоги защищены от случайного закрытия фоном или Escape.
- Оба операционных поля дней — аренды и продления — пусты при открытии. Все пять редактируемых дат используют маску `ДД.ММ.ГГГГ`, ограничение четырьмя цифрами года и последовательный Backspace справа налево; backend продолжает получать ISO.
- Шапка закреплена в однострочной компактной компоновке без внешних рамок сводки; сотрудник показан меньшим шрифтом, соединение подписано «Онлайн»/«Офлайн».
- Этап 11: проверены успешный комплект, сбой между копиями, отсутствие частичной публикации, повреждённая копия, неверный SHA-256/манифест, ротация 30, занятая база, недоступный путь и конкурентная запись.
- Этап 11: проверены постоянная блокировка записи при повреждении, отказ от неполного комплекта, сохранение повреждённых исходников, запрет восстановления при другом экземпляре и обезличенный аудит.
- Браузер: здоровые базы показывают `ok`, восстановление отключено; после искусственного повреждения тестового архива запись блокируется, кнопка включается, обе базы восстанавливаются, исходники сохраняются отдельно, новый комплект создаётся, ошибок консоли и горизонтального переполнения нет.
- Браузер: общий журнал показал три типа операций, ФИО клиента, границы продления, фильтры и кнопку Excel; исправления в список не попадают.
- Зависимости: Flask 3.1.3, openpyxl 3.1.5 и pytest 9.1.1 установлены только в `.venv`.

## Ожидает решения

- Q-006 нужен только перед проверкой на сетевом диске.

## Следующий этап

Этап 11.5 завершён. Следующий этап 12 не начинать без отдельного запроса пользователя. Перед производственным использованием остаются ручная визуальная сверка DOCX в Microsoft Word и проверка резервного копирования/восстановления на тестовом сетевом томе с 1–3 компьютерами.
