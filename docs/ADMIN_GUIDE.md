# Руководство администратора

## Статус документа

Это архитектурная версия. Точные команды и скриншоты появятся после реализации и сборки.

## Безопасные принципы развёртывания

- На каждом компьютере в папке установки находится собственный скрытый `safe-cells.exe` и локальный конфигурационный файл.
- На рабочем столе создаётся веб-ярлык «Сейфовые ячейки»; сотрудник использует только его.
- В конфиге указывается общая папка; реальный UNC-путь и пароли не коммитятся.
- В общей папке находятся `vault_cells.sqlite3`, `vault_archive.sqlite3`, `templates` и `backups`.
- Обе БД обязаны находиться на одном сетевом томе.
- Обычный запуск не создаёт отсутствующую базу. Первичная инициализация — отдельное подтверждённое действие.
- При первом запуске Windows-учётки полные фамилия и имя сотрудника сохраняются локально в `%LOCALAPPDATA%\SafeCells\employee-profiles.json`; файл не находится на сетевом диске.
- WAL запрещён; файловый атрибут Windows «Только чтение» не меняется.

## Первичная настройка — будущая процедура

1. Создать отдельную тестовую общую папку без реальных данных.
2. Проверить права чтения/записи и блокировки SMB с компьютеров сотрудников.
3. Выполнить явную инициализацию двух баз один раз.
4. Задать денежные настройки и административный пароль.
5. Установить утверждённые DOCX-шаблоны.
6. Запустить проверку с 1–3 экземплярами на тестовых данных.
7. Убедиться, что запуск с рабочего стола открывает браузер без окна консоли и без ручного поиска `.exe`.

## Бэкапы

После каждой подтверждённой записи приложение уже создаёт через SQLite Backup API один комплект из двух баз и проверяет обе копии через `quick_check`. Файлы помещаются в общую папку `backups`; сохраняются последние 30 полных пар. Обычное копирование активных файлов во время работы запрещено.

Манифесты, контрольные суммы, административный просмотр комплектов и контролируемое восстановление будут завершены на этапе 11. Отсутствие или ошибка бэкапа после сохранения договора показывается сотруднику отдельным предупреждением и требует обращения к администратору.

## Восстановление

Восстановление никогда не запускается автоматически. Администратор должен:

1. Закрыть все экземпляры приложения.
2. Сохранить повреждённые файлы отдельно.
3. Выбрать один полный проверенный комплект.
4. Восстановить обе базы вместе.
5. Выполнить проверку целостности и тестовый запуск.

Точные кнопки/команды будут добавлены после этапа 11. До этого документ не является инструкцией для производственного восстановления.

## Нерешённые входные данные

- фактический UNC-путь;
- подтверждение замены суммы залога прописью в распоряжении на закрытие;

## Технические команды тестовой версии этапа 1

Команды предназначены для разработчика или администратора тестового стенда, не для обычного сотрудника.

Явное создание тестовой пары баз:

```powershell
.\.venv\Scripts\python.exe -m app.cli init-db --config config.local.json --confirm INITIALIZE
```

Обычный запуск никогда не выполняет эту команду автоматически. Перед инициализацией папка уже должна существовать, а путь должен быть указан в локальном конфиге.

Явное обновление существующей тестовой пары со схемы 2 до схемы 3:

```powershell
.\.venv\Scripts\python.exe -m app.cli migrate-v3 --config config.local.json --confirm MIGRATE-TO-3
```

Команда сначала создаёт согласованный комплект резервных копий обеих баз. Обычный запуск миграцию не выполняет. Перед производственным обновлением все экземпляры приложения должны быть закрыты.
