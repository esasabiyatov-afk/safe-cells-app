Реализуй только первый этап рабочего приложения: каркас, конфигурацию, схемы баз, seed-данные и главный экран чтения. Не реализуй оформление аренды, продление, закрытие, редактирование, генерацию DOCX и административные настройки в этой задаче.

Перед началом прочитай:
- `AGENTS.md`;
- `docs/APPROVED_ARCHITECTURE.md`;
- утверждённые `docs/DECISIONS.md`;
- `docs/DATA_MODEL.md`;
- `docs/IMPLEMENTATION_PLAN.md`.

Если планирующие документы ещё не созданы или противоречат утверждённой архитектуре, не начинай кодирование. Сначала исправь только документацию и перечисли проблему.

Нужно сделать:

1. Создать модульную структуру Flask-проекта.
2. Добавить конфигурацию:
   - путь к общей сетевой папке;
   - имена рабочей и архивной баз;
   - отдельный безопасный локальный путь для тестов;
   - запрет автоматического создания локальной БД при недоступном производственном сетевом пути.
3. Создать идемпотентную инициализацию схем двух БД.
4. Для первого развёртывания предусмотреть явную команду инициализации общей базы. Обычный запуск не должен молча пересоздавать отсутствующую производственную базу.
5. Импортировать `data/cell_heights.csv`:
   - ровно 126 записей;
   - уникальные номера;
   - только высоты 50, 75, 100, 125, 175, 300;
   - повторный импорт не создаёт дубликатов.
6. Добавить seed для `vault_defaults`, тарифов и `config` из ТЗ.
7. Создать единый слой подключения к SQLite:
   - чтение через URI `mode=ro`;
   - короткое время жизни соединения;
   - `foreign_keys=ON` и `busy_timeout` для каждого соединения;
   - заготовка write-подключения с `journal_mode=DELETE`, `synchronous=FULL`, `locking_mode=NORMAL`, но без бизнес-операций записи в этом этапе.
8. Реализовать read-only API списка ячеек с вычисляемым статусом.
9. Реализовать главный экран на основе `reference/seyfy-prototip.html`:
   - точная высота в миллиметрах вместо категорий;
   - поиск по номеру;
   - фильтры по высоте и статусу;
   - счётчики состояний;
   - Windows-логин сотрудника в углу;
   - кнопка «Обновить»;
   - автоматическое обновление примерно раз в 15 секунд;
   - отсутствие случайных демонстрационных данных.
10. При недоступности сетевой базы показывать понятный экран ошибки, не создавая пустую локальную базу.
11. Не использовать интернет, CDN, Node.js и frontend-фреймворки.
12. Добавить `pytest`-тесты без реальных персональных данных.
13. Добавить README с простыми командами запуска, тестирования и первичной инициализации на Windows.

Обязательные проверки:
- `pytest` проходит;
- импортировано 126 уникальных ячеек;
- повторная инициализация не меняет количество записей;
- тарифы и настройки сохранены правильно;
- read-only соединение не выполняет запись;
- Flask test client получает список ячеек;
- страница открывается без внешних ресурсов;
- недоступный производственный путь не приводит к созданию локальной пустой базы.

После реализации:
- запусти тесты;
- покажи команды и точные результаты;
- перечисли изменённые файлы;
- дай пошаговую ручную проверку для пользователя без опыта программирования;
- сообщи, что намеренно не реализовано;
- не переходи к следующему этапу автоматически.
